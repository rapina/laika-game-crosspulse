# PRODUCTION LOG / 2026-07-22 / 교차파

## 콘셉트 잠금

- 질문: 내려오는 방울과 올라가는 파동을 흰 띠 한가운데서 만나게 할 수 있는가?
- 핵심 입력: 화면 탭. 키보드는 `Space` 또는 `Enter`. 입력 위치는 판정에 쓰지 않는다.
- 시스템 반응과 긴장 변화: 탭이 하단 진동판에서 압축파를 올려 보낸다. 4개 밴드에서 방울은 빨라지고 파동은 느려지며, 띠와 정밀 폭은 좁아지고 출현 리듬은 교대로 바뀐다.
- 재료: 우윳빛 반투명 탄성 젤, 진홍 염료 방울, 하단 진동판.
- 시각 매체: 생성 젤 질감을 샘플링하는 PixiJS WebGL 굴절·압축 변형과 판정 상태에서 파생한 파열 입자.
- 대표 색: 우윳빛 젤, 진홍, 백색, 보조 주황.
- 세계: 염료 방울을 지정 구간에서 터뜨리는 하나의 추상 장치.
- 마지막 장면: 마지막 판정 뒤 젤에 결과와 다음 탭 단서가 직접 남는다.
- 한 판 길이: 완주 55~70초, 방울 12개, 파열 3회면 즉시 종료. 무입력 약 18초.
- 제외: 이동·위치 조준·업그레이드·아이템·콤보 능력·분기·보스·서사 장면·별도 튜토리얼·랜덤 물리.

단발 탭으로 미래 교차점을 읽는 조작은 최근의 원형 각속도 드래그와 위치 드래그를 반복하지 않는다. 물·빛 대신 탄성 고체 내부의 압축파를 핵심 재료 반응으로 삼았다. 최근 학습은 두 개만 썼다. 첫 방울을 목표 가까이에 두되 무입력은 조기 파열시키고, 정밀 횟수와 이름 붙은 실패 원인을 화면에 남긴다.

## 목표 화면과 구현 대조

- 목표: `design/targets/first-play.png`, `verb-precision.png`, `verb-success.png`, `verb-failure.png`, `game-over.png`.
- 실제: `verification/captures/`의 같은 이름 5장.
- 첫 판 3답: 탭은 파동을 보내고, 예측점이 흰 선일 때 탭하며, 12개 판정 또는 파열 3회로 끝난다.
- 게임 오버 2답: `끝/END`로 종료가 구분되고 `화면을 탭해 재시작/TAP SCREEN TO RESTART`가 보인다.
- 의도적 차이: 목표 화면의 넓은 염료 광휘를 실제 화면에서는 얇은 굴절과 젤 변형으로 줄였다. 예측점·안전 띠·파열선의 대비를 먼저 읽히게 하기 위한 감산이다. 재료 질감과 정밀/성공/실패의 대칭 파열·비대칭 분리·세로 찢김은 유지했다.

## 감산 2회

1. 첫 패스: 광고·IAP·BGM 표면, 랭킹 버튼·기록 화면·리더보드 호출, 버전 표시, ko/en 외 로케일을 실제 플레이 경로에서 제거했다. `GameScreen`을 지연 import해 메인 gzip을 165.97kB에서 65.27kB로 낮췄다. 빈자리에는 기능을 넣지 않았다.
2. 둘째 패스: 쓰이지 않는 랭킹·모달·광고 배너 CSS와 셸 분기를 제거했다. 안내는 세 질문, 상시 HUD는 정밀·파열·진행, 결과는 마지막 오차 단서 하나만 남겼다. 확보한 시각 우선순위는 젤 굴절, 진동판 반동, 판정별 재료 반응에 두었다.

## 검증

- `npm test`: 4개 파일, 24개 테스트 통과. 고정 1/60 스텝, 네 밴드 경계, 입력 선처리, 가이드 복귀, 무입력 종료, 동일 시드 재현을 포함한다.
- `npx tsc -b`: 통과.
- `npm run build:web`: 통과. 메인 gzip 63.95kB, 게임 청크 gzip 91.33kB.
- `node scripts/playability-sim.mjs`: 각 정책 500판. 직관 완주 72.4%, 평균 판정 10.978, 정밀률 54.22%, 중앙 점수 820. 숙련 완주 97.2%, 평균 판정 11.906, 정밀률 86.96%, 중앙 점수 1100. 무입력 완주 0%, 평균 판정 3, 생존 분율 0.25, 정밀 0.
- 깊이 A/B: 같은 500시드·같은 오차/반응 표본에서 예측점 정책 평균 생존 11.686, 현재 방울이 띠에 들어온 뒤 반응하는 정책 7.29. 밴드별 정밀 격차는 39.73%, 65.15%, 72.57%, 55.43%, 후반 평균 격차 63.996%가 전반 52.441%보다 컸다.
- `npm run smoke`: `smoke-result.json`, `smoke.png`, `verification/captures/*.png`. 종료 결과 전달, 화면 탭 재시작, 첫 입력 전 무음, 입력 후 오디오 시작, 음소거, ko/en 전환, blur/focus 일시정지·복귀, 콘솔·페이지 오류 0을 확인한다.
- `npm run viewport`: `verification/viewport-result.json`과 12장 캡처. 360×800, 390×844, 430×932, 900×760의 standalone/portal 형상과 ko/en 게임 오버 잘림을 확인한다.
- `npm run build:arcade && npm run csp`: 실제 아케이드 CSP와 sandbox iframe에서 스타일·캔버스·릴리스 자산을 확인한다.
- 화면 정보 검증: 플레이 가능성 정책은 `renderModel`의 예측점·중앙선만 읽고 실제 `step`을 틱 단위 호출한다. 내부 정답 상태를 직접 호출하는 자동 완주와 구분한다.

- 최종 sourceHash: smoke·playability·viewport 모두 `eb296155342f4b49c11aef8850d8885a24990e36ab51724cd7144d9477f005b3`로 일치.
- viewport backing-store/CSS 최소 비율: 430×932 standalone에서 3.4023×3.4012, 요구 3.0 이상. 900×760 portal에서 3.0493×3.0491, 요구 2.0 이상. 8/8 형상 통과, ko/en 결과 잘림 0.
- arcade release: 14개 파일, 4,272,251 bytes, JS gzip 합계 283,461 bytes. CSP 위반·오류·누락 자산 0.

## 의존성 검사 예외

`package-lock.json`의 직접 의존성 20개를 Sonatype Guide에 검사하려 했으나 `No authentication token found in current context`로 거절됐다. 새 패키지를 추가·업그레이드하지 않았다. 이 저장소의 lockfile이 launchpad lockfile과 패키지 이름 외 완전히 같음을 비교한 뒤, 기존 `launchpad/node_modules`를 임시 symlink로 재사용했다. Sonatype 보안·정책 평가는 미완료다.

## 결과

- 상태: 제작 검증 통과, 잠금 준비 완료.
- 게임 잠금: 관제의 `prepare-editorial.mjs` 실행으로 생성한다.
- 알려진 위험: 무입력 생존 분율이 허용 경계인 정확히 0.25다. 포털 운영 환경의 실기 완주는 공개 단계에서 별도로 확인해야 한다.

## 런치패드 피드백

- 재사용 후보: 없음. 두 번째 사용처가 확인되지 않았다.
- 게임에 남길 코드: 1D 교차 규칙, 이미지 엘리먼트를 통한 CSP 안전 Pixi 텍스처 로더, 게임별 smoke 드라이버.
- 다음 게임에서 재검증할 항목: 캡처 전에 첫 판을 멈추지 않으면 예측점이 지나가 정밀 증거가 실패 장면으로 바뀐다. 첫 렌더 증거는 상태 정지 뒤 잡는다.

## 잠금 뒤 검증 추가

- 독립 설계 검토: `design-review.json`, 커밋 `4f009059650ebe7c035f5c25783e0c68201b55a5`, verdict `pass`, fatal gap 0.
- 실제 포인터 실기 관측: 직관 정밀 9/18, 성공 이상 15/18. 숙련 정밀 10/18, 성공 이상 14/18. 자연 제스처 정밀 8/18, 성공 이상 15/18.
- 무입력 실기: 3/12에서 파열 3회로 종료, 생존 분율 0.25, 정밀 0.
- 키보드: Space와 Enter가 각각 한 번의 판정만 만들고 자동 해결하지 않음. 결과 보호 시간 뒤 화면 탭 재시작 확인.
- 설계 검토 self-check: 세 프로필 모두 sound `true`; angle/magnitude ratio는 직관 1.03/1.10, 숙련 1.00/0.88, 자연 제스처 1.22/0.89.
